package com.demien.patterns.creational;

/* Actors: class with static instance 
   Goal: only one instance of object can be created and 
   used by other objects  */
public enum Singleton {
		INSTANCE;
		
		public String data;
		
}
